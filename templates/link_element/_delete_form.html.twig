<form method="post" action="{{ path('app_link_element_delete', {'id': link_element.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ link_element.id) }}">
    <button class="btn">Delete</button>
</form>
